# Classification-pipeline
Built an end-to-end machine learning classification pipeline to predict whether an individual will get their loan approved based on other variables. Used multiple models such as Logistic Regression, Random Forest, Neural Networks etc.
